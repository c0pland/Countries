name: Build and Test

on:
  push:
    branches:
      - '*'

jobs:
  build-and-test:
    runs-on: macOS-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build
      run: swift build

    - name: Test
      run: swift test
